{
  "version": 2,
  "builds": [
    {
      "src": "**",
      "use": "@vercel/static",
      "config": {
        "dist": "build/web"
      }
    }
  ],
  "scripts": {
    "build": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git -b 3.29.0 --depth 1; fi && export PATH=\"$PATH:flutter/bin\" && flutter doctor && flutter pub get && flutter build web --release"
  },
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
